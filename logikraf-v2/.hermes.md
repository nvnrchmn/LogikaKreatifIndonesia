# Aturan repo ini (auto-load saat bekerja di folder ini)

Baca dulu INDEX di `.hermesrules` root repo (baris awal saja) — JANGAN seluruh file.
Buka hanya section yang relevan dengan task.

- Deploy hanya lewat GitHub Actions (push `main`). Jangan build manual di server.
- Verifikasi = bukti nyata (kode HTTP, SHA commit, baris DB, keluaran tes). Belum diuji = katakan belum.
- Setiap task selesai: commit + push + tambah ≤20 baris ke `.hermesrules`.
- Jangan menulis nilai kredensial. Jangan `reboot`/`shutdown`. Jangan edit `/etc/nginx/nginx.conf`.
- LKI: env `DATABASE_DSN`; portal klien client.logikraf.id; portal mitra partners.logikraf.id.
